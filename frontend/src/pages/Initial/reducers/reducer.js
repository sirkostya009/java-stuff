const initialState = {
  availableItems: [
    'як+вивчити+js',
    'entities',
    'Картинка',
    'anotherPath',
  ],
};

export default (state = initialState, {type, payload}) => {
  switch (type) {
    default: return state;
  }
}
